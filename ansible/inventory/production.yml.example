# Gravity Platform Production Inventory
# Copy to production.yml and update with your values
#
# POC:        Single VM — use the default config below
# Enterprise: Uncomment the multi-VM section at the bottom

# =============================================================================
# POC — Single VM (all services on one host)
# =============================================================================
all:
  vars:
    starter_repo: "https://github.com/your-org/your-gravity.git"
    starter_branch: main

  hosts:
    gravity-prod:
      ansible_host: <YOUR_VM_IP>
      ansible_user: root   # DigitalOcean / direct root SSH

# If your VM uses a non-root user with passwordless sudo (Azure azureuser, AWS ubuntu, GCP debian):
# Do NOT add ansible_become_password or ansible_become_flags — cloud VMs have passwordless sudo by default.
# Adding ansible_become_password: "" will cause a 12s timeout error.
#
#   hosts:
#     gravity-prod:
#       ansible_host: <YOUR_VM_IP>
#       ansible_user: azureuser   # or ubuntu, debian, etc.

# =============================================================================
# ENTERPRISE — Multi-VM (uncomment below, remove the POC section above)
# =============================================================================
# all:
#   vars:
#     starter_repo: "https://github.com/your-org/your-gravity.git"
#     starter_branch: main
#
#   children:
#     app_vms:
#       hosts:
#         app-vm-1:
#           ansible_host: 10.0.1.10
#           ansible_user: root
#         app-vm-2:
#           ansible_host: 10.0.1.11
#           ansible_user: root
#       vars:
#         gravity_services:
#           - server
#           - workflow
#           - node-service
#           - mcp-server
#           - memory
#           - canvas
#
#     ml_vms:
#       hosts:
#         ml-vm-1:
#           ansible_host: 10.0.1.20
#           ansible_user: root
#       vars:
#         gravity_services:
#           - umap
