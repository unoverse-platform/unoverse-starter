/**
 * Shared constants for the AWS Nova package.
 *
 * The model ID is intentionally not configurable. The package always targets
 * the latest strongest Nova Sonic model. When AWS releases a new version,
 * update this single constant.
 */

export const NOVA_MODEL_ID = "amazon.nova-2-sonic-v1:0";
