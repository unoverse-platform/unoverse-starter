{
  "unoverse": "1.0",
  "kind": "atom",
  "name": "SectionHeader",
  "description": "Centered section heading: a title with an optional subtitle below. Owns its full style — references tokens only.",
  "whenToUse": "The heading for a step/section (e.g. a wizard question). Components COMPOSE this via a Ref, mapping title/subtitle to host fields.",
  "props": {
    "title": { "type": "string", "default": "Section title" },
    "subtitle": { "type": "string", "default": "Supporting subtitle" }
  },
  "root": {
    "type": "Box",
    "style": { "direction": "column", "gap": "1", "align": "center", "width": "full" },
    "children": [
      { "type": "Text", "bind": { "value": "title" }, "style": { "font": "headline.md", "weight": "semibold", "color": "text.primary" } },
      { "type": "Text", "visibleWhen": "subtitle", "bind": { "value": "subtitle" }, "style": { "font": "body.md", "color": "text.secondary" } }
    ]
  }
}
