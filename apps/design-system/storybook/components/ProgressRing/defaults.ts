/**
 * Default values for ProgressRing component
 * Used only in Storybook for demos and by templates' mock clients
 */

export const ProgressRingDefaults = {
  value: 48,
  max: 60,
  label: "Quarterly target",
  centerValue: "$48k",
  centerLabel: "of $60k goal",
  color: "var(--color-chart-1)",
  thickness: 12,
};
