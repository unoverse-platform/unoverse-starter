/**
 * ProgressRing Types
 * Auto-generated from Storybook component
 */

export interface ProgressRingConfig {
  /** Enable Focus Mode - allow component to expand as primary interaction surface */
  focusable?: boolean;
  /** Focus Mode Label - name shown in chat input when this component is focused */
  focusLabel?: string;
  value?: object;
  max?: object;
  label?: object;
  centerValue?: object;
  centerLabel?: object;
  color?: object;
  trackColor?: object;
  thickness?: object;
}

export interface ProgressRingOutput {
  __outputs: {
    componentSpec: any;
  };
}

export interface ProgressRingTemplate {
  componentUrl: string;
}
