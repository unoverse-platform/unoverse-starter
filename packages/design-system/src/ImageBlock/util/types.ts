/**
 * ImageBlock Types
 * Auto-generated from Storybook component
 */

export interface ImageBlockConfig {
  /** Enable Focus Mode - allow component to expand as primary interaction surface */
  focusable?: boolean;
  /** Focus Mode Label - name shown in chat input when this component is focused */
  focusLabel?: string;
  image?: string;
  alt?: string;
  caption?: string;
  object?: object;
}

export interface ImageBlockOutput {
  __outputs: {
    componentSpec: any;
  };
}

export interface ImageBlockTemplate {
  componentUrl: string;
}
