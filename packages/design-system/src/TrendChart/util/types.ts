/**
 * TrendChart Types
 * Auto-generated from Storybook component
 */

export interface TrendChartConfig {
  /** Enable Focus Mode - allow component to expand as primary interaction surface */
  focusable?: boolean;
  /** Focus Mode Label - name shown in chat input when this component is focused */
  focusLabel?: string;
  data?: object;
  series?: object;
  labels?: object;
  title?: object;
  color?: object;
  area?: object;
  showDots?: object;
  yMin?: object;
  yMax?: object;
  valuePrefix?: object;
  valueSuffix?: object;
}

export interface TrendChartOutput {
  __outputs: {
    componentSpec: any;
  };
}

export interface TrendChartTemplate {
  componentUrl: string;
}
